import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

/*class DropDownButtonOne extends StatelessWidget {

  String dropDownValue;
  List list;

  DropDownButtonOne({Key? key,
      required this.dropDownValue,this.list}); // DropDownButtonOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: DropdownButton(
        onChanged: (String? newValue) {
          dropdownValue = newValue;
        },
        value: dropDownValue,
        items: list
            .map((e) =>
            DropdownMenuItem(
              child: Text(e),
              value: e,
            ))
            .toList(),
      ),
    );
  }
}*/
